All the files here should be converted with these regexes:
* `s/(namespace)\s+SJ\.Tests\.Examples/\1 SJ.Examples/`
* `s/public\s+(static\s+void)\s+TMain/\1 Main/`

.. To be packed into the `master`/`main` branch..

> Perhaps in future the `master` branch could be the development and releases just could be on `release` branch or something like this (I need to pump up my green squares according to 38911 different sources).
